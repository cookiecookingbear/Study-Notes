# Accumulator (누산기)

- CPU가 계산 결과를 임시로 저장하거나, 연산을 이어서 수행하기 위해 사용하는 레지스터
- 누산기라는 이름 그대로 계산 중간 과정의 결과를 저장하여 사용한다
- 레지스터가 적었던 초기 CPU에서는 굉장히 중요한 역할을 수행했다.
- 레지스터 -> ALU -> Accumulator -> 메모리 순으로 작업 결과가 전달된다.

```csharp
int sum = 0;

for(int i=0;i<10;i++)
{
    sum += i;
}
```
에서  sum이 누산기의 역할을 한다.