# CS 기술면접 대비 학습 — 인수인계 문서

## 1. 프로젝트 목적

게임회사 취업을 목표로 한 기술면접 대비 학습. CS 전반과 C#, C++ 등을 공부하며,
학습한 내용을 GitHub 저장소(`cookiecookingbear/Study-Notes`)에 마크다운 노트로 정리한다.

- 사용자는 비전공자다. 이미 학습·습득한 내용이 있을 수 있으나, 의외로 기초적인 부분을 모를 수 있다.
- 설명에 잘못된 정보가 있어서는 절대 안 된다.

## 2. 사용자가 요청한 진행 방식

- 설명은 **가능한 한 상세하게**. 다만 **기술면접에서 다루는 범위를 넘어서지 않는 선**까지만.
- 각 항목 학습이 끝나면 **예상 질문**을 함께 제공한다.
  - 기존 `Interview_questions/` 노트와 같은 형식
  - 바로 복붙해서 .md 파일로 남길 수 있도록 **코드블록**으로 제공
  - 형식: `- 질문` 뒤에, 핵심 질문에 한해 `    > 모범 답변` 을 붙이는 방식
- 학습대기열에서 **"복습"이 붙은 항목**은 사용자가 기존에 작성해둔 .md 파일을 평가하고 보완하는 방식으로 진행한다.
- **"복습"이 없는 항목**은 새로 학습하며 노트를 작성한다.

## 3. 저장소 구조

- 루트 및 `CS/` 하위에 주제별 .md 노트가 존재한다.
- `Interview_questions/` 하위에 `<주제>_예상질문.md` 형태의 노트가 존재한다.
- 노트 간에는 상대경로 마크다운 링크로 상호 참조되어 있다. (예: `[PC](./ProgramCounter.md)`)
- `학습대기열.txt` 가 전체 학습 계획표 역할을 한다.

## 4. 현재 진행 상황

- **1~7번 완료**, 다음 차례는 **8번 「스레드」**.

### 완료 항목
1. 컴파일러와 인터프리터의 관계
2. lib 복습
3. dll 복습
4. 프로세스
5. PCB
6. ProgramCounter 복습
7. PCB_PC

## 5. 미반영 작업 (Claude Code에서 처리 필요)

아래는 채팅에서 내용은 다 만들었으나, 아직 저장소에 반영되지 않은 것들이다.

1. **`학습대기열.txt` 재정렬 반영**
   - 신규 항목 4개(12~15번)를 추가하고 이후 번호를 전부 +4로 재정렬한 버전이 필요하다.
   - 아래 6절의 전체 목록으로 덮어쓰면 된다.

2. **신규 노트 작성** — 아래 항목들은 학습을 마쳤으나 .md 파일이 아직 없다.
   - `프로세스.md`
   - `PCB.md`
   - `PCB_PC.md`
   - 각각에 대응하는 `Interview_questions/*_예상질문.md`

3. **`ProgramCounter.md` 보완**
   - 기존 노트의 다음 서술이 부정확하여 수정이 필요하다.
     > "MAR에는 현재 실행하는 명령어의 주소가 담기고, PC에는 다음 실행할 명령어의 주소가 담기는 것이 차이점이다."
   - MAR은 명령어 주소 전용이 아니라 Load/Store 등 데이터 접근에도 쓰이는 **범용 주소 레지스터**다.
     인출 단계에서 PC 값이 MAR로 복사되어 그 순간 값이 같아지는 것일 뿐이다.
     정확한 차이는 "PC는 다음 명령어 주소 전용 / MAR은 메모리 접근용 범용"이다.
   - 그 외 보완할 내용: PC 증가 시점(인출 직후), 아키텍처별 증가 폭(고정 길이 vs 가변 길이),
     PC가 비순차적으로 바뀌는 경우(분기/호출/복귀/인터럽트), PC 비트 수와 주소 공간의 관계.
   - 컨텍스트 스위칭 관련 내용은 `ProgramCounter.md`가 아니라 `PCB_PC.md`에서 다루기로 정리했다.
     `ProgramCounter.md`에는 한 줄 언급 + 링크만 남긴다.

## 6. 학습대기열 (재정렬 완료본)

```
1. 컴파일러와 인터프리터의 관계
2. lib 복습
3. dll 복습

4. 프로세스
5. PCB
6. ProgramCounter 복습
7. PCB_PC
8. 스레드
9. TCB
10. TCB_PC
11. 프로세스와 스레드
12. 프로세스 생성과 종료 (fork/exec, PID, 좀비·고아 프로세스)
13. 프로세스 간 통신 (IPC)
14. CPU 스케줄링 알고리즘
15. 동기화, 경쟁 상태, 데드락

16. 프로세스 메모리 영역
17. 코드·데이터·힙·스택 영역
18. 가비지 컬렉션 복습
19. 가상 메모리 복습
20. 가상메모리 페이징 복습
21. Demand Paging 복습
22. 가상메모리 관리정책 복습
23. 캐시 지역성 복습
24. 캐시 미스 복습

25. CPU Register
26. GPR·Flag Register·Stack Pointer
27. Accumulator 복습
28. Instruction Register 복습
29. MAR 복습
30. MBR/MDR 복습
31. CPU 제어장치
32. Decoder 복습
33. ALU 복습
34. 가산기·보수기·Shifter
35. Address·Data·Control Bus 복습
36. 명령어 사이클 복습
37. 인출 사이클 복습
38. 인터럽트 사이클 복습

39. CPU와 GPU 복습
40. GPU
41. RAM과 VRAM
42. GPGPU
43. Compute Shader
44. CUDA

45. 파일 포맷
46. BMP
47. JPG
48. PNG
49. TGA
50. EXR
51. WAV
52. AVI

53. 추상화
54. 인터페이스 복습
55. 추상 클래스 복습
56. 다형성 복습
57. 가상화 문서 재정리
58. UML 다이어그램 복습
59. 클래스 다이어그램 복습
60. 결합도 복습
61. CBD 복습
62. 소프트웨어 생명주기 복습
63. Verification 복습
64. Validation 복습
65. 해시코드 복습
```

## 7. 지금까지 학습한 내용 요약 (4~7번)

새 노트를 작성할 때 참고할 핵심 내용이다.

### 프로세스 (4번)

- 프로그램은 디스크에 저장된 정적인 파일, 프로세스는 그것이 메모리에 적재되어 실행 중인 인스턴스.
  같은 프로그램을 두 번 실행하면 프로세스는 두 개다. (클래스와 인스턴스의 관계에 비유 가능)
- 프로세스는 **운영체제가 자원을 나눠주는 단위**다. CPU 시간, 독립된 가상 주소 공간, 파일·소켓 등의 자원.
- 프로세스 상태: New → Ready → Running → Waiting(Blocked) → Terminated
- 코어가 하나여도 여러 프로세스가 동시 실행되는 것처럼 보이는 이유는 컨텍스트 스위칭 때문이다.
- 멀티프로그래밍(CPU 활용률 관점) vs 멀티태스킹(사용자 체감 관점) 구분.
- 프로세스는 최소 1개의 스레드를 가지며, 같은 프로세스의 스레드들은 코드·데이터·힙 영역을 공유한다.

### PCB (5번)

- PCB는 OS가 하나의 프로세스를 관리하기 위한 정보를 담는 **자료구조**다. ("공간"이 아니라 "자료구조"가 정확한 표현)
  - Linux의 `task_struct`, Windows의 `EPROCESS`가 실제 구현체이며 소스 상 구조체(struct)로 존재한다.
- 저장 정보: PID, 프로세스 상태, PC 값, 레지스터 값, 메모리 관리 정보(페이지 테이블 위치 등),
  스케줄링 정보(우선순위 등), 입출력 상태 정보(열린 파일 목록), 계정 정보(CPU 사용 시간 등)
- **프로세스 : PCB = 1:1** 이 개념적 원칙이다.
  - 단, 스레드 관리 구현은 OS마다 다르다.
    Linux는 프로세스와 스레드를 모두 `task_struct` 하나로 표현하며 `clone()`으로 자원 공유 여부를 플래그로 지정한다.
    Windows는 `EPROCESS`/`ETHREAD`로 명확히 분리되어 있어 교과서 모델(PCB 1개 + TCB 여러 개)에 가깝다.
- PCB는 커널 영역에 저장된다. 사용자 프로세스가 임의로 조작할 수 없어야 하기 때문이다.

### ProgramCounter (6번)

- PC는 CPU가 다음에 실행할 명령어의 메모리 주소를 담는 레지스터. x86의 IP/EIP/RIP, ARM의 PC.
- **본질은 주소 저장이 아니라 실행 흐름 제어**다. `if`, `for`, 함수 호출은 모두 기계어 레벨에서 PC 조작으로 구현된다.
- 인출 사이클: PC → MAR → 메모리 → MBR/MDR → IR, 그리고 PC 갱신.
- **PC는 명령어 실행이 끝난 뒤가 아니라 인출 직후에 증가한다.**
  그래서 CALL 실행 시점에 PC에는 이미 "다음 명령어 주소"가 들어있고, 이를 스택에 저장하면 반환 주소가 된다.
- PC vs MAR: PC는 명령어 주소 전용 / MAR은 명령어·데이터 모두를 위한 범용 주소 레지스터.
- 증가 폭: RISC(고정 길이)는 항상 일정(보통 +4), x86(가변 길이)은 인출한 명령어의 실제 길이만큼.
- PC가 순차 증가하지 않는 경우: 무조건 분기(Jump), 조건 분기(Branch, Flag Register 참조),
  함수 호출(Call, 반환 주소 스택에 push), 함수 복귀(Return, 스택에서 pop), 인터럽트/예외.
- 스택과의 연결: 재귀가 깊어지면 스택 오버플로우, 반환 주소를 덮어쓰는 것이 버퍼 오버플로우 공격의 원리.
- PC의 비트 수 = 표현 가능한 주소 공간의 크기. (32비트 → 2^32 = 4GB)
- PC에 담기는 주소는 물리 주소가 아니라 **가상 주소**이며, MMU가 물리 주소로 변환한다.
- PC는 보통 `mov` 등으로 직접 대입할 수 없고 `jmp`/`call`/`ret` 같은 제어 흐름 명령어로만 변경 가능하다. (안정성·보안)
  단, ARM처럼 PC를 범용 레지스터의 일부로 취급하는 아키텍처도 있다.

### PCB_PC (7번)

- CPU 코어당 물리적 PC 레지스터는 하나뿐이므로, 프로세스 전환 시 이전 PC 값을 저장해두지 않으면
  어디까지 실행했는지 알 수 없게 된다. 그래서 PCB(정확히는 TCB)에 저장한다.
- 컨텍스트 스위칭 흐름: 인터럽트 진입 → 스케줄러가 다음 프로세스 선택 → 현재 PC·레지스터를 PCB에 저장
  → 다음 프로세스의 PCB에서 복원 → 페이지 테이블 등 메모리 정보 전환 → 실행 재개.
- **PC만으로는 부족하다.** 중간 계산 결과(범용 레지스터), 스택 포인터, 플래그 레지스터 등이 함께 저장되어야 한다.
  이 묶음 전체를 **컨텍스트(Context)** 라고 한다.
- **실행 흐름의 단위는 프로세스가 아니라 스레드**이므로, 엄밀히 PC는 TCB에 저장된다.
  - PCB: 프로세스 전체가 공유하는 정보(PID, 메모리 맵, 열린 파일 목록, 권한)
  - TCB: 스레드별로 다른 정보(PC, 레지스터, 스택 포인터, 스레드 상태)
  - "PC는 PCB에 저장된다"는 설명은 단일 스레드 프로세스를 전제로 한 단순화다.
- **인터럽트 시 PC 저장 위치는 아키텍처마다 다르다.**
  - x86/x64: CPU가 하드웨어적으로 **커널 스택**에 RIP, CS, RFLAGS를 자동 push. `IRET`로 복귀.
    유저 스택을 쓰지 않는 이유는 유저 프로그램의 스택을 신뢰할 수 없기 때문이다.
    커널 스택 주소는 TSS에 등록되어 있다.
  - ARM64: `ELR_EL1`(복귀 주소), `SPSR_EL1`(프로세서 상태) 전용 레지스터에 저장. `ERET`로 복귀.
  - RISC-V: `mepc`/`sepc` CSR에 저장. `mret`/`sret`으로 복귀.
  - ARM·RISC-V 방식은 빠르지만 전용 레지스터가 하나뿐이라 중첩 예외 시 덮어쓰일 수 있어,
    핸들러 진입 직후 소프트웨어가 커널 스택으로 옮겨 담는다.
  - 공통점: 어느 쪽이든 커널 영역에 저장되며 유저 스택에는 저장되지 않는다.
- **인터럽트 시의 PC 저장 ≠ 컨텍스트 스위칭 시의 PC 저장.**
  전자는 같은 프로세스가 잠시 커널로 다녀오는 임시 저장, 후자는 실행 주체가 바뀌는 전환.
  인터럽트가 발생해도 대부분은 원래 프로세스로 곧바로 복귀하며, 타이머 인터럽트처럼
  스케줄러가 개입하는 경우에만 컨텍스트 스위칭으로 이어진다.
- 컨텍스트 스위칭 비용: 레지스터 저장/복원 자체보다 **캐시 오염, TLB 플러시, 파이프라인 플러시**가 더 크다.
  스레드 간 전환이 프로세스 간 전환보다 저렴한 이유는 주소 공간을 공유하므로
  페이지 테이블 교체와 TLB 플러시를 생략할 수 있기 때문이다.

## 8. 다음 할 일

**8번 「스레드」** 부터 학습을 이어간다.
(9번 TCB, 10번 TCB_PC는 위 7번 내용과 이어지므로 연속으로 진행하는 것이 자연스럽다.)
